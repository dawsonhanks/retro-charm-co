/**
 * Confirmed social profile links for RetroCharm Co.
 * Keep handles and URLs in sync with the live business accounts.
 */

export const instagram = {
  /** Confirmed business profile (no path suffixes). */
  url: 'https://www.instagram.com/theretrocharm.co/',
  handle: '@theretrocharm.co',
}

export const tiktok = {
  url: 'https://www.tiktok.com/@retrocharm.co',
  handle: '@retrocharm.co',
}

/** Profiles listed in Organization `sameAs` structured data. */
export const SOCIAL_PROFILES = [instagram.url, tiktok.url]
